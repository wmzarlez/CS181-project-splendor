#include "game.h"

Game::Game(){

}

void Game::run(){
    /*
    While(!state.is_win())
        for p in players:
            a=p.getAction(state)
            state.apply_action(a)
            check_noble(p)
    check who wins...
    */

}

void Game::train(){
    // for wmz
}